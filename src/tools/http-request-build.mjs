/* @generated by scripts/gen-tools.mjs —— 手改请同步到 tool-data 定义 */
export default {
  slug: 'http-request-build',
  name: 'HTTP 请求报文生成',
  desc: '生成原始 HTTP 请求报文：方法、路径、Host、请求头、请求体一键拼装。',
  keywords: 'http报文,请求报文生成,http请求构造,原始请求,报文模板',
  category: 'web',
  kind: 'calc',
  
  
  body: `<div class="calc-form">
<div class="field"><label for="ci-0">方法</label><select id="ci-0"><option value="GET" selected>GET</option><option value="POST">POST</option><option value="PUT">PUT</option><option value="DELETE">DELETE</option><option value="PATCH">PATCH</option></select></div><div class="field"><label for="ci-1">路径</label><input type="text" id="ci-1" value="/api/users" placeholder=""></div><div class="field"><label for="ci-2">Host</label><input type="text" id="ci-2" value="api.example.com" placeholder=""></div><div class="field"><label for="ci-3">请求头（每行一个）</label><textarea id="ci-3" rows="3">Accept: application/json</textarea></div><div class="field"><label for="ci-4">请求体（可选）</label><textarea id="ci-4" rows="2"></textarea></div>
</div>
<div class="toolbar">
  <button id="c-run" class="btn">计算</button>
  <span class="spacer"></span>
  <button data-copy-from="#c-out" class="btn btn-ghost btn-sm">复制结果</button>
</div>
<div class="output">
  <div class="output-label">计算结果</div>
  <pre id="c-out">等待输入…</pre>
</div>
<div class="note">输出 HTTP/1.1 原始报文，可用于 nc/telnet 调试或协议学习。</div>
<script type="application/json" id="c-cfg">{"lib":"web","fn":"rawRequest","unit":""}</script>`,
  usage: `<ol>
  <li>选择方法、填写路径、Host 与请求头，点击“计算”。</li>
  <li>输出 HTTP/1.1 原始报文（含空行与可选请求体）。</li>
  <li>可用 nc 或 telnet 直接发送，协议调试与教学场景。</li>
</ol>`,
  faq: [{"q":"报文能直接用吗？","a":"可配合 nc -v host 80 粘贴发送；HTTPS 需要 TLS，不能用明文报文。"},{"q":"Host 头必须吗？","a":"HTTP/1.1 必须带 Host 头（虚拟主机路由依据），本工具自动生成。"}],
};
