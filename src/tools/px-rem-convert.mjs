/* @generated by scripts/gen-tools.mjs —— 手改请同步到 tool-data 定义 */
export default {
  slug: 'px-rem-convert',
  name: 'px/rem/em 换算',
  desc: '像素单位换算：px 与 rem、em、pt、vw 互转，前端开发常用。',
  keywords: 'px转rem,rem换算,px em换算,字体单位换算,前端单位转换,px转vw',
  category: 'convert',
  kind: 'calc',
  
  body: `<div class="calc-form">
<div class="field">
  <label for="ci-0">px 值</label>
  <input type="number" id="ci-0" value="16" placeholder="">
</div><div class="field">
  <label for="ci-1">根字号（px，默认 16）</label>
  <input type="number" id="ci-1" value="16" placeholder="">
</div>
</div>
<div class="toolbar">
  <button id="c-run" class="btn">计算</button>
  <span class="spacer"></span>
  <button data-copy-from="#c-out" class="btn btn-ghost btn-sm">复制结果</button>
</div>
<div class="output">
  <div class="output-label">计算结果</div>
  <pre id="c-out">等待输入…</pre>
</div>
<div class="note">rem 基于根字号（html），em 基于父级字号（按 16px 估算）。</div>
<script type="application/json" id="c-cfg">{"lib":"convert-extra","fn":"pxConvert","unit":""}</script>`,
  usage: `<ol>
  <li>输入像素值与根字号（一般 16px），点击“计算”。</li>
  <li>输出 rem（除以根字号）、em（除以 16）、pt 与 1920 视口的 vw 值。</li>
  <li>写响应式 CSS、设计稿转代码时快速换算。</li>
</ol>`,
  faq: [{"q":"rem 和 em 的区别？","a":"rem 始终相对根元素（html）字号；em 相对父元素字号，会随嵌套累加。本工具 em 按父级 16px 估算。"},{"q":"vw 按什么视口算？","a":"默认按 1920px 宽度计算（1vw = 视口宽 1%）；实际值取决于用户屏幕，需自行调整。"}],
};
